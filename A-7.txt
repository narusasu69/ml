import pandas as pd
import numpy as np
import seaborn as sns
import matplotlib.pyplot as plt

df=pd.read_csv(r"E:\Desktop\DataSets\cancer_classification.csv")

df.head(5)

df.describe()

df.info()

correlation=df.corr()
sns.heatmap(correlation)
plt.title("Heatmap Table")

sns.countplot(x='benign_0__mal_1',data=df)

X= df.iloc[:, :-1].values
y= df.iloc[:, -1].values

from sklearn.model_selection import train_test_split
X_train,X_test,y_train,y_test = train_test_split(X,y,test_size=0.2,random_s
print("Scaled Training Set:")
print(X_train)
print("\nScaled Test Set:")
print(X_test)

from sklearn.preprocessing import StandardScaler
sc = StandardScaler()
X_train[:, 3:] = sc.fit_transform(X_train[:, 3:])
X_test[:, 3:] = sc.transform(X_test[:, 3:])

from sklearn.tree import DecisionTreeClassifier
classifier=DecisionTreeClassifier()
classifier.fit(X_train,y_train)

y_pred = classifier.predict(X_test)
print(np.concatenate((y_pred.reshape(len(y_pred),1),y_test.reshape(len(y_te

from sklearn.metrics import confusion_matrix , accuracy_score
cm=confusion_matrix(y_test,y_pred)
print(cm)
accuracy_score(y_test,y_pred)

classifier1=DecisionTreeClassifier(criterion='gini',max_depth=5,min_samples
classifier1.fit(X_train,y_train)

y_pred1= classifier1.predict(X_test)
print(np.concatenate((y_pred.reshape(len(y_pred1),1),y_test.reshape(len(y_t

cm=confusion_matrix(y_test,y_pred1)
print(cm)
accuracy_score(y_test,y_pred1)